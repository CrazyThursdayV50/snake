networks:
  default:
    name: general
    external: true

services:
  snake-data:
    container_name: ${APP}
    image: ${APP}:local
    env_file: ${CONTAINER_ENV_FILE}
    ports:
      - 33557:33557
    volumes:
      - /Users/alex/projects/alex/snake:/app/config
    networks:
      default:
        aliases:
          - snake
